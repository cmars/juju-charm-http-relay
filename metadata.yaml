name: http-relay
summary: Cross-model relay charm for http endpoints
maintainer: Casey Marshall <casey.marshall@canonical.com>
description: |
  Cross-model relay charm for http endpoints.
subordinate: false
requires:
  frontend:
    interface: http
    relay: some-api
provides:
  backend:
    interface: http
    relay: some-api
