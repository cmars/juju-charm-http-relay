name: http-relay
summary: cross-model relay for http interface
description: cross-model relay for http interface
requires:
  frontend:
    interface: http
    relay: some-api
provides:
  backend:
    interface: http
    relay: some-api
series:
  - xenial
